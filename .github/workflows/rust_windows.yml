name: Windows Build

on:
  push:
    branches:
      - windows

jobs:
  build_and_test_windows:
    name: Rust Windows Build
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
      - uses: actions-rs/cargo@v1
        with:
          command: build
          args: --release --all-features
      - name: Remove Redundant
        run: |
          Remove-Item -Recurse -Path '.\target\release\.fingerprint'
          Remove-Item -Recurse -Path '.\target\release\build'
          Remove-Item -Recurse -Path '.\target\release\deps'
          Remove-Item -Path '.\target\release\.cargo-lock'
          Remove-Item -Path '.\target\release\onlyone.d'
          Remove-Item -Path '.\target\release\onlyone.pdb'
          Remove-Item -Path '.\target\.rustc_info.json'
          Remove-Item -Path '.\target\release\onlyone_windows.exe'
      - name: Rename Bin
        run: |
          Rename-Item -Path '.\target\release\onlyone.exe' -NewName 'onlyone_windows.exe'
      - name: Push Changes
        run: |
          git pull origin windows
          git config --global user.name "Lateralus138"
          git config --global user.email "faithnomoread@yahoo.com"
          git add .
          git commit --allow-empty -m "Windows Build Test Complete"
          git push -u origin windows --force
      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: True
          tag: 1.0.0.0
          name: Stable Release
          prerelease: False
          artifacts: "target/release/onlyone_windows.exe"
          token: ${{ secrets.GITHUB_TOKEN }}